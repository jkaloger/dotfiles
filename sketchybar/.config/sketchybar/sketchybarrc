#!/bin/bash

THEME_STATE="$HOME/.local/state/sketchybar/theme"
THEME="$(cat "$THEME_STATE" 2>/dev/null || echo spaceduck)"
[ -d "$CONFIG_DIR/themes/$THEME" ] || THEME=spaceduck

export THEME_DIR="$CONFIG_DIR/themes/$THEME"
export PLUGIN_DIR="$CONFIG_DIR/plugins"

source "$THEME_DIR/init.sh"
